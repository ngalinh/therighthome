services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks: [app-net]

  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./storage:/app/storage
      - ./secrets:/app/secrets:ro
    networks: [app-net]
    # Bind to a host port so an existing host nginx can reverse-proxy to it.
    # Default 127.0.0.1:3002 means only host nginx (or local) can reach it.
    # When using bundled nginx (profile=bundled-proxy), this is still bound
    # but nginx container talks to it via the docker network as well.
    ports:
      - "${APP_HOST_PORT:-127.0.0.1:3002}:3000"
    expose:
      - "3000"

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
      app:
        condition: service_started
    command: ["node", "scripts/worker.js"]
    networks: [app-net]

  nginx:
    profiles: ["bundled-proxy"]
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./certbot/www:/var/www/certbot:ro
      - ./certbot/conf:/etc/letsencrypt:ro
    depends_on: [app]
    networks: [app-net]

  certbot:
    profiles: ["bundled-proxy"]
    image: certbot/certbot:latest
    volumes:
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf:/etc/letsencrypt
    entrypoint: /bin/sh -c "trap exit TERM; while :; do certbot renew --webroot -w /var/www/certbot --quiet; sleep 12h & wait $${!}; done"
    networks: [app-net]

  backup:
    build:
      context: ./scripts/backup
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./secrets:/app/secrets:ro
      - ./backups:/app/backups
    networks: [app-net]

volumes:
  postgres-data:

networks:
  app-net:
    driver: bridge
